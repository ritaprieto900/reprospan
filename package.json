{
  "name": "reprospan",
  "version": "0.0.0",
  "private": true,
  "description": "Replay a failed agent run, change one step, and prove the fix.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.13.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run check",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
