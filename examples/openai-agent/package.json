{
  "name": "@reprospan/example-openai-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm --filter @reprospan/sdk build && pnpm --filter @reprospan/adapter-openai build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@reprospan/adapter-openai": "workspace:*",
    "@reprospan/sdk": "workspace:*",
    "openai": "^4.90.0"
  }
}
